
const express = require('express');
let router = express.Router();



const express = require('express');
const router = express.Router();

router.get('/', (req,res) => {
});
router.get('/:genero', (req,res) => {
});
router.get('/crear', (req,res) => {
});

module.exports = router;



const express = require('express');
const app = express();

let rutasSeries = require('./routes/series');