<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../includes/head")%>
    <link href="/static/css/styleLogIn.css"  rel="stylesheet">
 

</head>

<body>
    <%- include("../includes/header")%>

    <main>
            <h4>Inicio de sesión</h4>       
            <form action="/users/acceder" method="POST">
            <p><label for="email">Correo:</label>
                <input 
                type="email" 
                placeholder="noeg@gmail.com" 
                id="correo" 
                name="correo"
                class=" <%= locals.errors && errors.email ? "invalid" : null %> "
                >
                <% if (locals.errors && errors.email) { %>
                    <section class="text-danger">
                        <%= errors.name.msg %> 
                    </section>
                <% } %>
            </p>
            <p><label for="password">Contraseña:</label>
                <input 
                type="password" 
                placeholder="security password" 
                id="pass" 
                name="password"
                class=" <%= locals.errors && errors.password ? "invalid" : null %> "
                >
            </p>
            <button onclick="location.href='/'">
                Ingresar
            </button>
            <p class="registro">¿Sos nuevo? <a href="/users/register">¡Registrate!</a></p>
            </form>

    </main>
    
    <%- include("../includes/footer")%>  

</body>
</html>